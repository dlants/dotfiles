// Entry point for magenta script discovery. Magenta forks only this file, so it
// must import every script module to run their registerScript() calls.
import "./code-review.ts";
import "./implement-plan.ts"; // h
