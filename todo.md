# glean

- hunks and files marked as "seen" should go into a separate, expandable section. One per file.
- marking something as seen should also collapse it
- ephemeral split diff view (new buffer, should still be able to leave comments on the new buffer)
- what if we leave a comment on a deleted / context line?
- interaction w/ magenta - sending to magenta should attach context - which file, which sha (if not current), line ranges. Maybe just yanking in general?
- interaction w/ magenta - when finished reviewing, we should be able to produce an overall "review" which lists all of our comments, along with file/line locations. Probably just display this at the bottom of the Glean buffer so we can easily yank it.
- github integration?
